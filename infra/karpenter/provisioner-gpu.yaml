# GPU PROVISIONER DISABLED FOR FREE TIER
# GPU instances (g5.xlarge) cost ~$1/hour minimum
# With $180 credits and other infrastructure costs, GPU nodes are not feasible
#
# TO ENABLE IN PRODUCTION:
# Uncomment this file and apply:
#   kubectl apply -f infra/karpenter/provisioner-gpu.yaml
#
# apiVersion: karpenter.sh/v1beta1
# kind: Provisioner
# metadata:
#   name: gpu-provisioner
# spec:
#   limits:
#     resources:
#       nvidia.com/gpu: 2    # hard cap — max 2 GPUs for cost control
#   requirements:
#     - key: "karpenter.k8s.aws/instance-category"
#       operator: In
#       values: ["g"]
#     - key: "karpenter.sh/capacity-type"
#       operator: In
#       values: ["spot"]   # spot only
#   ttlSecondsAfterEmpty: 30