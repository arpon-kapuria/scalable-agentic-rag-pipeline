model_config:
  model_id: "BAAI/bge-m3"
batch_size: 8              # ← reduced from 32 (less RAM)
normalize_embeddings: true
dtype: "float32"           # ← changed from float16 (float16 requires GPU)
max_seq_length: 512        # ← reduced from 8192 (saves memory on CPU)